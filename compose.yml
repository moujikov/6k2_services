name: 6k2-services

services:
  traefik:
    container_name: traefik
    image: traefik:v3
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    env_file: .env
    environment:
      - TIMEWEBCLOUD_AUTH_TOKEN_FILE=/run/secrets/timeweb_auth_token
    volumes:
      - certificates:/usr/local/share/traefik/certs
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik:/etc/traefik:ro
    labels:
      - "traefik.enable=true"

      - "traefik.http.routers.traefik-static.service=api@internal"
      - >
        traefik.http.routers.traefik-static.rule=
        Host(`traefik.6k2.ru`) && (
        PathPrefix(`/static/`) ||
        PathRegexp(`/manifest\.json$`) || 
        PathRegexp(`/favicon\.(ico|png|svg)$`) )

      - "traefik.http.routers.traefik.service=api@internal"
      - "traefik.http.routers.traefik.middlewares=ip-whitelist@file,admin-auth@file"
      - >
        traefik.http.routers.traefik.rule=
        Host(`traefik.6k2.ru`)
    secrets:
      - admins
      - timeweb_auth_token


  database:
    container_name: database
    image: postgres:18-alpine
    restart: unless-stopped
    volumes:
      - ./database/init:/docker-entrypoint-initdb.d
      - database:/var/lib/postgresql
    env_file: .env
    environment:
      - POSTGRES_PASSWORD_FILE=/run/secrets/db_password_postgres
    secrets:
      - db_password_postgres
      # change ownership of db users passwords to postgres (uid 70, gid 70)
      # since postgres system user will need to read passwords in order to create db users
      - source: db_password_authelia
        uid: '70'
        gid: '70'
    healthcheck:
      test: pg_isready -U postgres
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
      start_interval: 1s
    deploy:
      resources:
        limits:
          memory: 1G


  watchtower:
    container_name: watchtower
    image: nickfedor/watchtower
    restart: unless-stopped
    env_file: .env
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_POLL_INTERVAL=60
      - WATCHTOWER_LABEL_ENABLE=true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /root/.docker/config.json:/config.json


  portainer:
    container_name: portainer
    image: portainer/portainer-ce:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.portainer.loadbalancer.server.port=9000"
      - "traefik.http.routers.portainer.service=portainer"
      - "traefik.http.routers.portainer.middlewares=ip-whitelist@file"
      - >
        traefik.http.routers.portainer.rule=
        Host(`portainer.6k2.ru`)


  glances:
    container_name: glances
    image: nicolargo/glances:latest
    restart: unless-stopped
    env_file: .env
    environment:
      - "GLANCES_OPT=-w"
    pid: host
    volumes:
      - /:/rootfs:ro
      - database:/database:ro
      - portainer_data:/portainer_data:ro
      - utility-bills_data:/utility-bills_data:ro
      - ./glances:/etc/glances:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /etc/os-release:/etc/os-release:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.glances.loadbalancer.server.port=61208"
      - "traefik.http.routers.glances.service=glances"
      - "traefik.http.routers.glances.middlewares=ip-whitelist@file"
      - >
        traefik.http.routers.glances.rule=
        Host(`glances.6k2.ru`)


  oss-2021:
    container_name: oss-2021
    image: moujikov/6k2_oss-2021
    restart: unless-stopped
    env_file: .env
    labels:
      - "traefik.enable=true"
      - >
        traefik.http.routers.oss-2021.rule=
        Host(`2021.oss.6k2.ru`) || 
        Host(`2021.xn--n1aga.xn--62-2lc.xn--p1ai`) ||
        Host(`2021.xn--b1ayja.xn--p1ai`)


  oss-2023:
    container_name: oss-2023
    image: moujikov/6k2_oss-2023
    restart: unless-stopped
    env_file: .env
    labels:
      - "traefik.enable=true"
      - >
        traefik.http.routers.oss-2023.rule=
        Host(`oss.6k2.ru`) || 
        Host(`2023.oss.6k2.ru`) || 
        Host(`xn--n1aga.xn--62-2lc.xn--p1ai`) ||
        Host(`2023.xn--n1aga.xn--62-2lc.xn--p1ai`) || 
        Host(`xn--b1ayja.xn--p1ai`) || 
        Host(`2023.xn--b1ayja.xn--p1ai`)


  osch-2024:
    container_name: osch-2024
    image: moujikov/6k2_osch-2024
    restart: unless-stopped
    env_file: .env
    labels:
      - "traefik.enable=true"
      - > 
        traefik.http.routers.osch-2024.rule=
        Host(`2024.os4.6k2.ru`) || 
        Host(`2024.xn--n1ags.xn--62-2lc.xn--p1ai`)


  osch-2025:
    container_name: osch-2025
    image: moujikov/6k2_osch-2025
    restart: unless-stopped
    env_file: .env
    labels:
      - "traefik.enable=true"
      - > 
        traefik.http.routers.osch-2025.rule=
        Host(`os4.6k2.ru`) || 
        Host(`2025.os4.6k2.ru`) || 
        Host(`xn--n1ags.xn--62-2lc.xn--p1ai`) ||
        Host(`2025.xn--n1ags.xn--62-2lc.xn--p1ai`)


  utility-bills:
    container_name: utility-bills
    image: moujikov/6k2_utility-bills
    restart: unless-stopped
    env_file: .env
    volumes:
      - utility-bills_data:/var/app-data
    labels:
      - "traefik.enable=true"
      - > 
        traefik.http.routers.utility-bills.rule=
        Host(`bills.6k2.ru`) || 
        Host(`bills.var62.ru`) 


  alt-chat:
    container_name: alt-chat
    image: moujikov/6k2_alt-chat
    restart: unless-stopped
    env_file: .env
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
      - "traefik.enable=true"
      - >
        traefik.http.routers.alt-chat.rule=
        Host(`a4.6k2.ru`)
    secrets:
      - users

volumes:
  certificates:
  database:
  portainer_data:
  utility-bills_data:


secrets:
  admins:
    file: /usr/local/share/traefik/auth/admins
  users:
    file: /usr/local/share/traefik/auth/users
  timeweb_auth_token:
    file: /usr/local/share/traefik/auth/timeweb_auth_token
  db_password_postgres:
    file: /usr/local/share/database/auth/db_password_postgres
  db_password_authelia:
    file: /usr/local/share/database/auth/db_password_authelia
