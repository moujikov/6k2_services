http:

  middlewares:
    admin_auth:
      digestAuth:
        removeHeader: true
        usersFile: /run/secrets/admins
    user_auth:
      digestAuth:
        removeHeader: true
        usersFile: /run/secrets/users
